# 📦 Python Functions

## Defining
Use `def` keyword.
```python
def greet(name):
    return f"Hello, {name}"
```

## Arguments
- **Positional**: `func(1, 2)`
- **Keyword**: `func(a=1, b=2)`
- **Default**: `def func(a=1)`

## Lambda
Anonymous functions.
`add = lambda x, y: x + y`
